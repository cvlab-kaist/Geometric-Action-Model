# =============================================================================
# GAM for LIBERO / LIBERO-Plus training and closed-loop inference
#
# These are the public Python dependencies for a CUDA GPU machine with Python
# 3.12. Reproduce with either:
#   - Docker:  docker build -t gam-libero .        (recommended; see Dockerfile)
#   - conda :  conda env create -f environment.yml
#   - venv  :  python3.12 -m venv .venv && . .venv/bin/activate &&
#              pip install -r requirements.txt
#
# Additional setup steps are handled by the Dockerfile or README:
#   1) torch / torchvision: install the build matching your CUDA/platform FIRST
#      (see the command below). Everything here is compatible with torch >= 2.5.
#   2) The LIBERO benchmark + the DA3 backbone: installed from source; see README
#      / Dockerfile. Install LIBERO with `--no-deps` because its own dependency
#      pins conflict with this release.
# =============================================================================

# ---- torch ------------------------------------------------------------------
# Public-compatible with torch >= 2.5. The GAM predictor uses
# torch.nn.attention.flex_attention / BlockMask.
# Install the wheel for YOUR platform BEFORE the rest, e.g.:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124
# The Dockerfile uses the pytorch/pytorch:2.5.1-cuda12.4 base, so torch is
# already present there.
torch>=2.5
torchvision

# ---- core deep-learning stack ----------------------------------------------
transformers==5.5.4
tokenizers==0.22.2
huggingface_hub==1.10.1
safetensors==0.5.3
accelerate==1.13.0
datasets==4.8.4
timm==1.0.26
einops==0.8.1
deepspeed==0.18.8

# ---- numerics / config / IO ------------------------------------------------
numpy==1.26.4
scipy==1.15.3
h5py==3.16.0
omegaconf==2.3.0
antlr4-python3-runtime==4.9.3
pillow==11.2.1
# robosuite can pull opencv-python with a system libGL dependency. On a headless
# server, force headless cv2 after install. Docker and scripts/setup_sources.sh
# do this:
#   pip uninstall -y opencv-python opencv-python-headless && \
#   pip install --no-deps opencv-python-headless==4.11.0.86
opencv-python-headless==4.11.0.86
imageio==2.37.3
imageio-ffmpeg==0.6.0
av==15.1.0
matplotlib==3.10.3
moviepy==1.0.3
plotly==5.24.1
tqdm==4.67.1
protobuf==4.24.4
wandb==0.24.2

# ---- closed-loop simulator eval (LIBERO + LIBERO-Plus) ----------------------
# MuJoCo + robosuite + gymnasium drive the LIBERO rollout. robosuite 1.4.1 is
# the version LIBERO targets. numba/llvmlite back
# robosuite's controller math; PyOpenGL provides the EGL bindings for headless
# rendering (set MUJOCO_GL=egl).
mujoco==3.6.0
robosuite==1.4.1
gymnasium==1.2.3
cloudpickle==3.1.1
Farama-Notifications==0.0.4
numba==0.60.0
llvmlite==0.43.0
PyOpenGL==3.1.10
gym==0.26.2
scikit-image==0.25.2
Wand==0.6.13

# ---- LIBERO benchmark python deps (the LIBERO package itself is source-installed
#      with --no-deps; these are the only runtime deps its env classes need) ----
bddl==3.6.0
easydict==1.13
future==1.0.0
